старт работы - найти ссылку и склонировать проект себе
git clone https://github.com/JackD2020/open_snipet.git

---------=============
у меня была проблема что не распознавал сертификат банка в котором был битбакет через впн

Проблема в том, что git по умолчанию использует криптографический сервер "Linux".

Начиная с Git для Windows 2.14, теперь вы можете настроить Git на использование SChannel,
 встроенного сетевого уровня Windows в качестве криптографического бэкэнда. 
 Это означает, что вы будете использовать механизм хранения сертификатов Windows,
 и вам не нужно явно настраивать механизм хранения curl CA: https://msdn.microsoft.com/en-us/library/windows/desktop/aa380123(v=vs.85).aspx

Просто выполните:

git config --global http.sslbackend schannel
Это должно помочь.

Использование schannel на данный момент является стандартной настройкой при установке git для Windows,
 также рекомендуется не проверять репозитории с помощью SSH, если это возможно, 
 поскольку https легче настроить и с меньшей вероятностью будет 
 заблокирован брандмауэром, что означает меньшую вероятность сбоя.

------======

git checkout -b Gutorov
Switched to a new branch 'Gutorov'

после добавления или изменения
git commit

закидываем на сервер
git push -u origin Gutorov

сливаем из mster в gutorov
git merge master

удаление:
из удаленного репозитория:
	git push origin --delete Gutorov

Удаляет локальную ветку, если уже сделан её пуш и мааердж:
	git branch -d branch_name
Принудительно (force) удаляет локальную ветку, несмотря ни на что:
	git branch -D branch_name



Список локальных веток
Чтобы вывести список локальных веток используется команда:
git branch

Список удаленных веток
Чтобы показать удаленные (remote) ветки используется ключ -r:
git branch -r

Перед тем, как выполнять данную команду, можно сначала обновить удаленные ветки у себя в репозитории, для этого используется команда:
git fetch
git fetch загружает коммиты, файлы и ссылки из удаленного репозитория. Данная команда выполняется, когда вы хотите посмотреть,
 что изменилось в удаленном репозитории, что кто-то другой сделал в нем.
 При этом очень важно, что git fetch не изменяет никаких ваших локальных данных, над которыми вы работаете.


Список всех веток
Для вывода всех веток, локальных и удаленных, используется ключ -a:
git branch -a

Чтобы переключиться на ветку в Git используется команда git checkout.
 Например, чтобы переключиться на ветку master необходимо выполнить команду:
git checkout master

